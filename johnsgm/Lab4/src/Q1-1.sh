#!/bin/sh
# This script is a template for the Q1-1

clear				# clear terminal window
#echo "The script starts now."

# grep --help 
#chmod 777 *.sh

# You need to modify the following code.... Good luck!
grep -ER '(^| )[A-Za-z_+.\-]+@([a-zA-Z0-9\-]+\.)+[a-zA-Z]{2,6}($| )' ./